# Angular State Management

The frontend heavily utilizes reactive state principles.

## Managing State
We leverage heavily typed Observables mapped through Singleton feature-services to provide unidirectional data bounds ensuring templates read clean representations without triggering redundant backend querying routines.
